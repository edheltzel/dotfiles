" Make lf replace netrw and be the file explorer
let g:NERDTreeHijackNetrw = 0 "// add this line if you use NERDTree
let g:lf_replace_netrw = 1 "// open lf when vim open a directory

" Disables default key mapping
let g:lf_map_keys = 0
